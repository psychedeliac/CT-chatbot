# ── LLM & Agent ───────────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-classic>=1.0.0
langchain-google-genai>=2.0.0
langchain-chroma>=0.2.0
langchain-huggingface>=0.1.0
# Only needed if LLM_PROVIDER=groq (imported lazily by core/llms.py):
# langchain-groq>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Retrieval Pipeline (RRF fusion + cross-encoder rerank) ────────────────────
rank-bm25>=0.2.2
sentence-transformers>=5.0.0
torch>=2.0.0

# ── PII Guardrails ────────────────────────────────────────────────────────────
# Required ONLY when PII_ENABLED=true. Without the spaCy model, presidio tries
# to auto-download it at startup and the process dies with exit code 2 and no
# traceback. `python -m spacy download` can fail behind a uv-backed pip shim,
# so install the wheel directly:
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.40.0

# ── Data Handling ─────────────────────────────────────────────────────────────
pandas>=2.0.0
tqdm>=4.66.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0          # BeautifulSoup(html, "lxml") in every scraper needs this
